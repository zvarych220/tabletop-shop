# Специфікація та план розробки вебзастосунку «Dice & Deck»
## Навчальний проєкт курсової роботи з дисципліни «Розробка вебзастосунків на ReactJS»

---

## 1. Вступ та аналіз предметної області

### 1.1. Тема проєкту
Вебзастосунок спеціалізованого магазину та клубу настільних ігор **«Dice & Deck»**.

### 1.2. Проблема предметної області
Настільні ігри — це складний для підбору товар. Покупцеві важливо враховувати специфічні критерії: точну кількість гравців, мінімальний віковий поріг, час однієї партії, механіку (кооперативна, стратегія, паті-гра) та рівень складності.

Зараз інформація про наявність ігор, їхні правила, комплектацію та вартість у локальних клубах і магазинах часто розпорошена між соцмережами, каталогами видавництв і чатами. Це ускладнює:
- швидкий вибір гри новачками без консультанта;
- пошук ігор для конкретної компанії (наприклад, суворо «для 2 гравців» або «для вечірки від 6 осіб»);
- прозоре бронювання та оформлення замовлень для самовивозу або доставки.

### 1.3. Мета розробки
Спроєктувати та розробити швидкий, компонентно-орієнтований вебзастосунок на базі ReactJS, який надає зручний каталог із фільтрацією, детальні картки ігор, інтерактивний кошик і форму замовлення з клієнтською валідацією.

---

## 2. Модель предметної області (Сутності та дані)

У межах системи визначено чотири ключові сутності:

### 2.1. Настільна гра (`BoardGame`)
Головна сутність застосунку.
- `id` (*string*, унікальний): стабільний ідентифікатор запису (наприклад, `'game-001'`);
- `title` (*string*): комерційна назва гри;
- `category` (*string*): жанр / категорія (Стратегія, Паті-гра, Кооперативна, Сімейна, Детектив);
- `players` (*object* або *string*): діапазон гравців (`min: 1, max: 4` або `"1-4 гравці"`);
- `playTime` (*string*): орієнтовна тривалість партії (наприклад, `"45-60 хв"`);
- `minAge` (*number*): рекомендований віковий поріг (наприклад, `10+`);
- `complexity` (*number*): оцінка складності за шкалою від 1 (легка) до 5 (важка);
- `price` (*number*): актуальна вартість у національній валюті (UAH);
- `inStock` (*boolean*): поточний стан доступності на складі;
- `stockQuantity` (*number*): фізичний залишок коробок;
- `description` (*string*): короткий анотаційний опис сюжету, цілей і базових механік гри;
- `imageUrl` (*string*): шлях до зображення коробки гри (локальний або CDN).

### 2.2. Категорія / Жанр (`Category`)
Використовується для категоризації та побудови фільтрів.
- `id` (*string*): ідентифікатор категорії (`'cat-strategy'`, `'cat-party'`);
- `name` (*string*): назва категорії, що відображається в інтерфейсі;
- `description` (*string*): коротке пояснення жанру.

### 2.3. Позиція замовлення / Кошика (`CartItem`)
Проміжна сутність, що зв'язує гру з наміром купівлі.
- `gameId` (*string*): посилання на ідентифікатор гри;
- `quantity` (*number*): кількість обраних одиниць;
- `priceAtPurchase` (*number*): зафіксована ціна на момент додавання.

### 2.4. Замовлення / Заявка на бронь (`Order`)
Формується на етапі оформлення покупки (Л 3.1).
- `id` (*string*): унікальний номер замовлення (`'ord-2026-001'`);
- `customerName` (*string*): повне ім'я покупця;
- `customerPhone` (*string*): контактний номер телефону;
- `customerEmail` (*string*): поштова адреса для сповіщень;
- `deliveryType` (*enum: 'pickup' | 'courier' | 'post'*): спосіб отримання;
- `deliveryAddress` (*string*, optional): адреса або відділення доставки;
- `items` (*Array<CartItem>*): масив замовлених позицій;
- `totalAmount` (*number*): фінальна сума замовлення;
- `status` (*enum: 'new' | 'confirmed' | 'completed' | 'cancelled'*): стан обробки;
- `createdAt` (*string / ISO*): дата та час створення заявки.

---

## 3. Групи користувачів та аналіз потреб

| Група користувачів | Портрет і потреби | Ключовий сценарій |
| :--- | :--- | :--- |
| **Гість / Відвідувач** | Людина без облікового запису; шукає гру для подарунка чи компанії, порівнює ціни й наявність. | Швидкий перегляд каталогу, читання анотацій, оцінка статусу наявності. |
| **Покупець (Геймер)** | Досвідчений гравець або постійний клієнт; чітко знає бажані параметри (час, складність). | Фільтрація за жанром, додавання товарів у кошик, оформлення бронювання. |
| **Менеджер / Адміністратор** | Співробітник магазину; керує залишками на складі та опрацьовує вхідні заявки. | Зміна статусу доступності гри (`inStock`), обробка черги нових замовлень. |

---

## 4. Деталізовані сценарії використання (Use Cases)

### Сценарій 1: Базовий перегляд каталогу та перевірка наявності (Реалізовано в Л 1.1)
- **Користувач:** Гість / Відвідувач.
- **Передумова:** Користувач відкрив головну сторінку застосунку в браузері.
- **Основний потік подій:**
  1. Користувач потрапляє на Головну сторінку та бачить промо-блок із призначенням магазину.
  2. Переходить до секції `#catalog`.
  3. Застосунок відображає сітку карток настільних ігор з локального масиву даних.
  4. Користувач переглядає назву гри, жанр, рекомендовану кількість гравців, час партії та вартість.
  5. Користувач звертає увагу на текстовий статус доступності: *"В наявності"* або *"Очікується поставка"*.
- **Результат:** Користувач поінформований про поточний асортимент та ціни.
- **Альтернативний потік (Каталог порожній):**
  - Якщо масив ігор порожній (`boardGames.length === 0`), система замість сітки карток відображає інформативне повідомлення: *"Наразі каталог порожній. Завітайте пізніше або зверніться до менеджера."*

### Сценарій 2: Фільтрація каталогу за жанром та кількістю гравців (Заплановано на Л 2.1 / Л 3.1)
- **Користувач:** Покупець.
- **Передумова:** Користувач перебуває в каталозі товарів.
- **Основний потік подій:**
  1. Користувач обирає категорію (наприклад, «Стратегія»).
  2. Вказує бажану кількість гравців (наприклад, «2»).
  3. Інтерфейс миттєво фільтрує масив і оновлює відображені картки без перезавантаження сторінки.
- **Результат:** Відображено лише ігри, що відповідають усім критеріям.

### Сценарій 3: Перегляд детальної картки гри та правил (Заплановано на Л 2.2)
- **Користувач:** Гість / Покупець.
- **Передумова:** Відкрито каталог ігор.
- **Основний потік подій:**
  1. Користувач натискає на назву або картку гри.
  2. Маршрутизатор перенаправляє його на URL виду `/games/:gameId`.
  3. Відкривається окрема сторінка з розширеним описом, фотографіями компонентів, рівнем складності та відгуками.
- **Результат:** Користувач отримує повну інформацію для прийняття рішення про покупку.

### Сценарій 4: Оформлення замовлення / бронювання (Заплановано на Л 3.1)
- **Користувач:** Покупець.
- **Передумова:** У кошик додано хоча б одну гру зі статусом `inStock === true`.
- **Основний потік подій:**
  1. Користувач переходить на сторінку `/checkout`.
  2. Заповнює поля форми: ПІБ, телефон, спосіб доставки.
  3. Система валідує введені дані (наявність обов'язкових полів, формат номера телефону).
  4. Користувач підтверджує замовлення.
  5. Створюється об'єкт сутності `Order`, а кошик очищується.
- **Результат:** Замовлення зареєстровано, користувач бачить екран успішного оформлення з номером замовлення.

---

## 5. Функціональні вимоги та Roadmap реалізації

План поступового розвитку застосунку відповідно до структури лабораторного практикуму:

| Вимога до функціоналу | Перевірюваний технічний результат | Етап реалізації |
| :--- | :--- | :--- |
| **Презентація системи та предметної області** | Відображається промо-секція з назвою та описом функцій магазину. | **Л 1.1 (Виконано)** |
| **Базове відображення асортименту** | Картки формуються з масиву об'єктів; відображаються атрибути та різні стани наявності. | **Л 1.1 (Виконано)** |
| **Контейнеризація середовища** | Налаштовано `Dockerfile` та `compose.yaml`; сервіс запускається на порту 5173. | **Л 1.1 (Виконано)** |
| **Компонентна декомпозиція** | Виділення повторно використовуваних компонентів: хедер, футер, бейдж статусу, сітка. | **Л 1.2** |
| **Керування клієнтським станом** | Реалізація фільтрації через React State (`useState`), збереження обраних ігор у закладки. | **Л 2.1** |
| **Багатосторінкова навігація** | Інтеграція React Router: маршрути `/`, `/games`, `/games/:gameId`, `/cart`. | **Л 2.2** |
| **Форми замовлення та валідація** | Створення форми заявки, валідація номерів, повідомлення про помилки вводу. | **Л 3.1** |
| **Асинхронна робота з даними** | Винесення логіки в сервісний шар, симуляція API-запитів, індикатори завантаження (Spinners). | **Л 3.2** |
| **Оптимізація та фіналізація** | Обробка крайових випадків, мемоізація, підготовка до захисту курсової. | **Л 4.1** |

---

## 6. Архітектура сторінок та маршрутизація

| URL-шлях | Назва сторінки | Призначення | Стан реалізації |
| :--- | :--- | :--- | :--- |
| `/` | Головна сторінка | Промо-блок, популярні новинки, початковий каталог (секція `#catalog`). | **Реалізовано в Л 1.1** |
| `/games` | Каталог ігор | Повний список із сайдбаром фільтрації за жанрами та ціною. | Заплановано на Л 2.2 |
| `/games/:gameId` | Сторінка гри | Повні характеристики, опис правил, детальні фото, кнопка «Купити». | Заплановано на Л 2.2 |
| `/cart` | Кошик | Список вибраних ігор, зміна кількості, підрахунок суми. | Заплановано на Л 3.1 |
| `/checkout` | Оформлення | Форма контактних даних та адреси доставки з валідацією. | Заплановано на Л 3.1 |

---

## 7. Поточна структура коду (Л 1.1)

```text
tabletop-shop/
├── .dockerignore            # Виключення для контексту збірки образу Docker
├── Dockerfile               # Інструкції побудови Node.js контейнера
├── compose.yaml             # Конфігурація сервісу та монтування томів
├── package.json             # Залежності проєкту (React, Vite)
├── docs/
│   └── project-plan.md      # Архітектурна специфікація проєкту
├── src/
│   ├── components/
│   │   └── BoardGameCard.jsx # Компонент картки окремої гри (UI відображення)
│   ├── data/
│   │   └── boardGames.js     # Початкові тестові дані (Mock Data)
│   ├── pages/
│   │   └── HomePage.jsx      # Компонент головної сторінки з каталогом
│   ├── App.jsx              # Кореневий компонент-оболонка (Header + Page + Footer)
│   ├── main.jsx             # Вхідна точка React (createRoot)
│   └── index.css            # Базові стилі та адаптивна сітка Grid
└── README.md                # Інструкція з нативного та контейнерного запуску