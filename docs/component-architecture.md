# Архітектура компонентів (Лабораторна робота 1.2)

Цей документ описує компонентну ієрархію, відповідальності та контракти властивостей (props) вебзастосунку «Магазин настільних ігор Dice & Deck».

## 1. Ієрархія компонентів

Дерево відображення компонентів побудоване за принципом композиції:

- **`App`** (Кореневий компонент застосунку)
  - **`AppLayout`** (Спільна оболонка)
    - `SiteHeader` (Шапка сайту)
      - `MainNav` (Навігаційне меню за якірними посиланнями)
    - `<main>` (Основний семантичний контейнер вмісту `#main-content`)
      - **`HomePage`** (Сторінка каталогу)
        - `Section` (Секція "Про магазин")
        - `Section` (Секція "Каталог настільних ігор")
          - `CatalogSummary` (Підсумок загальної кількості ігор)
          - `BoardGameList` (Список настільних ігор)
            - `BoardGameCard` (Картка окремої гри)
              - `AvailabilityBadge` (Статус наявності гри на складі)
            - `EmptyState` (Виводиться, якщо каталог порожній)
      - **`OrderPage`** (Макет швидкого замовлення / бронювання)
        - `Section` (Секція оформлення замовлення)
          - `AvailabilityBadge` (Статус наявності обраної для замовлення гри)
          - `OrderFormPreview` (Макет форми замовлення)
            - `FormField` (Компонування полів вводу з підписами та підказками)
            - `AppButton` (Кнопки дій: очистити / оформити)
          - `EmptyState` (Виводиться, якщо гру не вибрано)
    - `<footer>` (Підвал застосунку)

---

## 2. Контракти ключових компонентів (Props)

| Компонент | Вхідні властивості (Props) | Домовленість та відповідальність |
| :--- | :--- | :--- |
| **`AppLayout`** | `title`, `links`, `children` | Створює спільну оболонку (посилання переходу до вмісту, шапку, навігацію, контейнер `<main>` та підвал). |
| **`Section`** | `id`, `title`, `children` | Формує семантичну секцію з унікальним `id`, доступним заголовком `<h2>` (`aria-labelledby`) та отримує контент через `children`. |
| **`MainNav`** | `links` | Приймає масив об'єктів з полями `href` та `label` для побудови списку навігації. |
| **`BoardGameList`** | `items` | Обов'язковий масив ігор. Допускає порожній масив (`[]`), у такому разі відображає `EmptyState`. |
| **`BoardGameCard`** | `game` | Коректний об'єкт гри з моделі даних (`id`, `title`, `category`, `players`, `playTime`, `price`, `inStock`). Відповідає суто за її візуалізацію. |
| **`AvailabilityBadge`** | `available` | Логічне значення наявності (`boolean`). Єдине джерело правил текстового статусу («В наявності» / «Очікується поставка») та кольорового маркування. |
| **`CatalogSummary`** | `total` | Числове значення загальної кількості товарів у каталозі. |
| **`OrderPage`** | `selectedGame` | Об'єкт настільної гри або `undefined` (передбачає випадок, коли гру для замовлення ще не вибрано). |
| **`OrderFormPreview`** | `idPrefix`, `gameTitle` | Префікс для унікалізації `id` полів форми та зафіксована назва гри у полі лише для читання (`readOnly`). |
| **`FormField`** | `id`, `label`, `hint`, `children` | Обгортка поля форми. Забезпечує доступний зв'язок підпису (`label htmlFor`) та підказки (`hint`) з дочірнім елементом вводу. |
| **`AppButton`** | `children`, `type`, `variant`, `disabled` | Універсальна кнопка дій. Підтримує варіанти оформлення `primary` / `secondary` та блокування (`disabled`). |
| **`EmptyState`** | `title`, `children` | Універсальне повідомлення про відсутність даних (для порожнього каталогу або невибраного товару). |

---

## 3. Обґрунтування головних рішень

1. **Винесення `AvailabilityBadge` в окремий компонент:** Правило визначення наявності гри (`inStock`) використовується як у картках каталогу, так і в блоці підтвердження замовлення. Винесення його в окремий компонент усуває дублювання логіки та забезпечує єдиний вигляд статусів по всьому застосунку.
2. **Використання композиції (`children`):** Такі компоненти, як `AppLayout`, `Section` та `FormField`, приймають контент через `children`. Це робить їх універсальними та дозволяє змінювати внутрішню структуру сторінок чи форм без переписування каркаса.
3. **Розділення відповідальності списку та картки (`BoardGameList` і `BoardGameCard`):** Список відповідає за ітерацію масиву (`.map()`), стабільні ключі `key={game.id}` та перевірку на порожній масив (`EmptyState`), тоді як картка залишається «чистим» компонентом відображення одного предмета.
4. **Статичний макет форм:** Форма замовлення реалізована з перехопленням події подання (`onSubmit={(e) => e.preventDefault()}`), а кнопка замовлення заблокована (`disabled`), оскільки реальне збереження, робота зі станом та валідація будуть реалізовані в наступних лабораторних роботах (Л 2.1 та Л 3.1).